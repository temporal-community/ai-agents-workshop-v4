[project]
name = "ai-agents-webinar-intro"
version = "0.1"
description = "OpenAI Agents SDK + Temporal integration demo"
authors = [{ name = "Temporal Technologies Inc", email = "sdk@temporal.io" }]
requires-python = ">=3.10"
readme = "README.md"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]
