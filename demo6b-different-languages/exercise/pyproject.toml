[project]
name = "demo6b-different-languages"
version = "0.1"
description = "Heterogeneous multi-agent orchestration across languages: Python orchestrator + Java (Spring AI) travel planner behind one Temporal orchestrator"
authors = [{ name = "Temporal Technologies Inc", email = "sdk@temporal.io" }]
requires-python = ">=3.10"
readme = "README.md"
license = "MIT"
dependencies = [
    "temporalio[openai-agents,opentelemetry]>=1.17.0,<2",
    "httpx>=0.27",
]
